apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-frontend
  namespace: rag-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rag-frontend
  template:
    metadata:
      labels:
        app: rag-frontend
    spec:
      containers:
        - name: frontend
          image: maddy30/rag-frontend:v2
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: rag-frontend-service
  namespace: rag-app
spec:
  selector:
    app: rag-frontend
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
